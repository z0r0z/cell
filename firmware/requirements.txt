# Gate logic and calibration. Runs anywhere — no hardware needed.
numpy>=1.24
scipy>=1.10

# Seed storage. AES-256-GCM via OpenSSL, so the seed is authenticated as well
# as encrypted and the cipher is not a hand-rolled one. On Raspberry Pi OS this
# is `apt install python3-cryptography`.
cryptography>=41.0

# Everything else in the signing stack — secp256k1, BIP-32/39, PSBT, RLP,
# RIPEMD-160, Keccak-256 — is pure Python in this directory, with no third
# party dependency, and is checked against the published test vectors by
# run_tests.py.

# Optional second opinions, for firmware/test_consensus.py and
# firmware/test_drivers.py. Not needed to run the firmware, and not needed for
# the suites to pass — both skip when these are absent. Install them to have
# an independent implementation check our output rather than only our own
# assertions:
#   pip install python-bitcointx coincurve cryptoauthlib
#
# Device only, Raspberry Pi. Uncomment when building the hardware.
# Sensors and the secure element:
# adafruit-blinka
# adafruit-circuitpython-as7341
# picamera2
# cryptoauthlib
#
# The screen, the buttons and the QR camera. Everything above these runs
# against protocols, so the firmware and its tests work without them.
# adafruit-circuitpython-rgb-display
# pillow
# qrcode
# gpiozero
# opencv-python
