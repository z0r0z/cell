__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# calibration output — device- and person-specific, never commit
captures/
thresholds.json

.DS_Store

# transfer artefacts
*.zip

# scratch pages written by tools/render_turntable.py, tools/export_model.py and
# tools/render_social_card.py while headless Chrome drives the viewer. Removed
# on exit, but a killed run leaves one behind and it must never be committed.
viewer/__turntable.html
viewer/__export.html
viewer/__card.html

# foundry
contracts/out/
contracts/cache/
contracts/lib/

# --soft rehearsal only: the software secure element's secret, in the clear.
SOFT-SE-INSECURE.json
