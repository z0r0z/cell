{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "cmd=$(jq -r '.tool_input.command // \"\"'); blanket='(^|[;&|(]|[[:space:]])git[[:space:]]+add([[:space:]]+-[^;&|[:space:]]+)*[[:space:]]+(-A|--all|\\.)([[:space:]]|$|[;&|)])'; commit_a='(^|[;&|(]|[[:space:]])git[[:space:]]+commit([[:space:]]+[^;&|[:space:]]+)*[[:space:]]+(-a|-a[a-zA-Z]+|--all)([[:space:]]|$|[;&|)])'; if printf '%s' \"$cmd\" | grep -Eq \"$blanket|$commit_a\"; then printf '%s' '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"Blanket staging is blocked in this repo. Several Claude sessions share this working tree, so git add -A / git add . / git commit -a commit whatever another session has half-edited - it has already shipped bugs three times (see CLAUDE.md). Stage explicit paths instead: git add path/one path/two\"}}'; fi",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
